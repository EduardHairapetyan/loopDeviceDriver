# loopDeviceDriver
Linux kernel driver for character device that creates /dev/loop device that loops the input into /tmp/output file. Works with all types of files.
Has been tested on files with size up to 10GB under Linux kernel version 6.12.57 running on Debian 13.
